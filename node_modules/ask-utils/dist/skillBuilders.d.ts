import { CustomSkillBuilder } from 'ask-sdk';
import { SkillConstants } from '@ask-utils/handlers';
import { SkillConfig, SkillHandlers } from './models';
export declare const createSkill: <T extends SkillConstants = SkillConstants>(config: SkillConfig<T>, handlers: SkillHandlers) => CustomSkillBuilder;
