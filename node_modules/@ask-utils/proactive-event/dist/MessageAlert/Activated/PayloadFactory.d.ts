import { interfaces } from '../../model';
import MessageAlert = interfaces.MessageAlert;
import PayloadBuilder = MessageAlert.Activated.PayloadBuilder;
export default class ParameterFactory {
    static init(): PayloadBuilder;
}
