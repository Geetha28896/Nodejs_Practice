import { RequestHandler, HandlerInput } from 'ask-sdk-core';
import { Response } from 'ask-sdk-model';
export declare type CanHandle = (handlerInput: HandlerInput) => Promise<boolean> | boolean;
export declare type Handle = (handlerInput: HandlerInput) => Promise<Response> | Response;
export declare class HandlerBuilder {
    protected canHandleFilter: CanHandle;
    canHandle: CanHandle;
    handle: Handle;
    setHandleCondition(condition: CanHandle): this;
    setHandle(handle: Handle): this;
    updateHandler(condition: CanHandle, handle: Handle): this;
    replaceHandler(handler: Partial<RequestHandler>): this;
    getHandler(): RequestHandler;
}
export declare class HandlerFactory {
    static init(): HandlerBuilder;
}
export default HandlerFactory;
