import { HandlerInput } from 'ask-sdk-core';
import { Response } from 'ask-sdk-model';
import { HandlerBuilder, CanHandle } from './fixtures';
export declare class BuyIntentHandlerBuilder extends HandlerBuilder {
    canHandle: CanHandle;
    static create(): void;
}
export declare const BuyIntentHandler: {
    canHandle(handlerInput: HandlerInput): boolean;
    handle(handlerInput: HandlerInput): Promise<Response>;
};
export default BuyIntentHandler;
