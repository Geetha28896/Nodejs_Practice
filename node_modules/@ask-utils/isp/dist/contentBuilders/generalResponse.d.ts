import { ResponseBuilder } from 'ask-sdk-core';
import { Response, services } from 'ask-sdk-model';
import monetization = services.monetization;
import InSkillProduct = monetization.InSkillProduct;
declare type InskillProducts = InSkillProduct[];
export declare const makeParagraphText: (messages: string[]) => string;
export declare const UnSupportedISPResponse: (locale: string) => {
    speechText: string;
    repromptText: string;
};
export declare const NoProductResponse: (responseBuilder: ResponseBuilder, locale: string, products?: InskillProducts | undefined) => Response;
export {};
