import { ResponseBuilder } from 'ask-sdk-core';
import { Response, Directive } from 'ask-sdk-model';
export interface ResponseContent {
    speechText?: string;
    repromptText?: string;
    directives?: Directive[];
}
export declare class ContentBuilder {
    protected locale: string;
    protected responseBuilder: ResponseBuilder;
    protected contents: ResponseContent;
    constructor(locale: string, responseBuilder: ResponseBuilder);
    protected isJP(): boolean;
    putSpeechText(text: string): this;
    putRepromptText(text: string): this;
    putDirectives(directives: Directive[]): this;
    putDirective(directive: Directive): this;
    getResponse(): Response;
}
export default ContentBuilder;
