import { RequestHandler, HandlerInput } from 'ask-sdk-core';
import { interfaces, Request, Response, RequestEnvelope } from 'ask-sdk-model';
export declare const cloneHandler: (cloneTarget: RequestHandler) => RequestHandler;
interface MergeObjectType {
    canHandle?: (handlerInput: HandlerInput) => boolean;
    handle?: (handlerInput: HandlerInput) => Response;
}
export declare const mergeHandler: (cloneTarget: RequestHandler, mergeObject: MergeObjectType) => RequestHandler;
export declare const isSkillConnectionResponse: (request: Request) => request is interfaces.connections.ConnectionsResponse;
export declare const isSkillConnectionRequest: (request: Request) => request is interfaces.connections.ConnectionsRequest;
export declare const isSkillEvent: (requestEnvelope: RequestEnvelope) => boolean;
export {};
