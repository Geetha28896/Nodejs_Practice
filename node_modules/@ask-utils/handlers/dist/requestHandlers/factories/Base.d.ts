import { RequestHandler } from 'ask-sdk-core';
import { CanHandle, Handle, HandlerBuilderInterface } from './model';
export declare class HandlerBuilder implements HandlerBuilderInterface {
    protected canHandleFilter: CanHandle;
    protected handlerFilter: Handle;
    canHandle: CanHandle;
    handle: Handle;
    setHandleCondition(condition: CanHandle): this;
    setHandle(handle: Handle): this;
    updateHandler(condition: CanHandle, handle: Handle): this;
    replaceHandler(handler: Partial<RequestHandler>): this;
    getHandler(): RequestHandler;
}
export default HandlerBuilder;
