import { RequestHandler, HandlerInput } from 'ask-sdk-core';
import { Response } from 'ask-sdk-model';
export declare type CanHandleResponse = Promise<boolean> | boolean;
export declare type HandleResponse = Promise<Response> | Response;
export declare type CanHandle = (handlerInput: HandlerInput) => CanHandleResponse;
export declare type Handle = (handlerInput: HandlerInput) => Promise<Response> | Response;
export interface HandlerBuilderInterface {
    canHandle: CanHandle;
    handle: Handle;
    setHandleCondition(condition: CanHandle): HandlerBuilderInterface;
    setHandle(handle: Handle): HandlerBuilderInterface;
    updateHandler(condition: CanHandle, handle: Handle): HandlerBuilderInterface;
    replaceHandler(handler: Partial<RequestHandler>): HandlerBuilderInterface;
    getHandler(): RequestHandler;
}
