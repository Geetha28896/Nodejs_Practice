import { services } from 'ask-sdk-model';
import Client from './client';
export declare class SettingAPIClient extends Client {
    protected path: string;
    getTimezone(deviceId: string): Promise<string>;
    getTempratureUnit(deviceId: string): Promise<services.ups.TemperatureUnit>;
    getDistanceUnits(deviceId: string): Promise<services.ups.DistanceUnits>;
}
