import { services } from 'ask-sdk-model';
import Client from './client';
export declare class UserProfileAPIClient extends Client {
    path: string;
    getProfileName(): Promise<string>;
    getGivenName(): Promise<string>;
    getEmail(): Promise<string>;
    getMobileNumber(): Promise<services.ups.PhoneNumber>;
}
