import { AxiosRequestConfig } from 'axios';
import { RequestEnvelope } from 'ask-sdk-model';
declare type Method = 'GET' | 'PUT' | 'POST' | 'DELETE';
export declare abstract class APIClient {
    protected token: string;
    protected locale: string;
    protected endpoint: string;
    protected path: string;
    protected isDebug: boolean;
    constructor(requestEnvelope: RequestEnvelope, isDebug?: boolean);
    protected getURL(path?: string): string;
    protected createAxiosRequestObject(method: Method, path?: string, data?: any): AxiosRequestConfig;
    protected delete(path?: string): Promise<any>;
    protected get(path?: string): Promise<any>;
    protected post(body?: any, path?: string): Promise<any>;
    protected put(body?: any, path?: string): Promise<any>;
}
export default APIClient;
